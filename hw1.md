## 项目实践一：Sky Plane Battle 飞机大战（基础版）

### 零、项目概览

#### 0.1 学习目标

本作业以 `game1/` 中已经拆分好的 Pygame 项目为基础。
你需要阅读现有项目结构，根据代码中的 `TODO` 注释，分三个层次逐步补全一款可运行的飞机大战游戏。

完成本作业后，你应能够：
1. 理解 Pygame 游戏的“事件处理 → 状态更新 → 碰撞检测 → 画面绘制”主循环。
2. 使用类描述玩家、敌机和子弹，并理解对象之间如何协作。
3. 使用列表管理数量不断变化的游戏对象，并及时清理失效对象。
4. 使用矩形碰撞和像素遮罩碰撞实现游戏规则。
5. 使用游戏状态组织开始、运行、结束和重新开始流程。
6. 阅读并维护由多个 Python 文件组成的小型项目。

#### 0.2 项目结构与模块职责
| 文件 / 路径 | 类型 | 主要职责与功能 |
|---|---|---|
| `assets/` | 资源目录 | 存放游戏素材，包括玩家飞机、敌机、子弹等图片资源 |
| `game1/main.py` | 程序入口 | 游戏启动文件，调用 `game.py` 中的 `main()` 方法运行游戏 |
| `game1/game.py` | 游戏核心模块 | 负责 Pygame 初始化、游戏主循环、事件处理、对象管理、碰撞检测、分数统计以及游戏状态控制 |
| `game1/settings.py` | 配置模块 | 集中管理游戏参数，包括窗口尺寸、速度、颜色、自定义事件、状态名称和文本等配置 |
| `game1/player.py` | 玩家模块 | 定义 Player 玩家飞机类，实现图片加载、移动控制、边界限制、位置更新、绘制和发射子弹等功能 |
| `game1/enemy.py` | 敌机模块 | 定义 Enemy 敌机类，实现敌机随机生成、移动、越界判断和绘制等功能 |
| `game1/bullet.py` | 子弹模块 | 定义 Bullet 子弹类，实现子弹创建、向上移动、越界判断和绘制等功能 |
| `game1/__init__.py` | 初始化文件 | 标识 `game1` 为 Python 模块包 |
| `hw1.md` | 项目文档 | 记录项目说明、作业要求和开发相关信息 |

#### 0.3 环境配置与操作指南

运行项目需要安装 `pygame` 库。

如果尚未安装，请先执行：

```bash
pip install pygame
```

在项目根目录 `Sky-Plane-Battle/` 下运行：

```bash
python game1/main.py
```

> 注意：需要从项目根目录启动程序，否则 `assets/...` 等相对路径可能无法正确加载游戏资源。

---

游戏启动后，玩家可以进行以下操作：

* **方向键 / `W A S D`**：控制玩家飞机移动。
* **开始界面按任意键**：进入游戏。
* **游戏结束界面按任意键**：重新开始游戏。
* **关闭游戏窗口**：退出程序。
* **自动射击机制**：基础版本采用定时自动发射子弹，不需要玩家手动按键射击。

#### 0.4 开发要求与进阶规则

1. 只补全 `TODO`注释指定的代码，不要删除已有类、函数、参数或随意改变文件职责。
2. 每完成一个层次，都应先运行和测试，再继续下一层。
3. 不允许直接复制完整参考项目或把所有逻辑重新写进一个文件。
4. 可以增加必要的局部变量和辅助注释，但应保持命名清楚、缩进正确。
5. 程序运行期间不应因为普通操作产生异常。
6. 第一层是最低完成要求；第二层建立在第一层之上；第三层建立在前两层之上。

### 一、v1.1：玩家移动与基础游戏循环
完成玩家飞机和基本运动，建立一个能够持续运行的游戏循环。
完成后，玩家可以控制飞机，在界面内上下左右移动，并允许飞机左右两侧适当超出屏幕。

#### 1.1 实现玩家方向控制

涉及文件：`player.py`

需要完成：

1. 在 `Player.move(keys)` 中读取上下左右方向键和 `W/A/S/D`。
2. 左右移动改变 `self.x`，上下移动改变 `self.y`。
3. 使用 `self.speed` 作为每次更新的移动量。
4. 同时按下两个方向键时，允许斜向移动。

提示：`pygame.key.get_pressed()` 返回当前所有按键的状态；方向键常量如 `pygame.K_LEFT`，字母键如 `pygame.K_a`。

#### 1.2 限制玩家移动范围

涉及文件：`player.py`

需要完成：
1. 实现 `Player.stay_in_screen()`。
2. 玩家飞机对象上边不能小于 `0`；左侧可以超出 `PLAYER_OFFSET`。
3. 右侧边界需要考虑玩家飞机对象宽度和 `PLAYER_OFFSET`。
4. 下侧边界需要考虑玩家飞机对象高度。
5. 在 `update()` 中按正确顺序调用移动和边界限制；代码已提供 `rect` 的位置同步。

提示：左右两侧允许适当超出屏幕，超出距离由 `PLAYER_OFFSET` 控制。

#### 1.3 将玩家接入游戏主循环

涉及文件：`game.py`

将玩家对象接入游戏主循环。
1. 创建玩家对象
2. 根据按键更新玩家
3. 绘制玩家

---

完成后运行程序，确认：

- 玩家飞机显示在窗口底部中央。
- 可以使用方向键或 `W/A/S/D` 移动飞机。
- 玩家飞机不会从上下边界移出屏幕，左右两侧最多超出 `PLAYER_OFFSET`。

### 二、v1.2：敌机生成与对象管理
敌机会从屏幕上方随机出现并向下飞行。

#### 2.1 实现敌机创建与移动

涉及文件：`enemy.py`

需要完成：

1. 随机设置敌机的横坐标，范围为 `0` 到 `SCREEN_WIDTH - self.rect.width`；同时将敌机放在屏幕上方。
2. 将敌机的初始位置保存到 `self.x`、`self.y`，并同步到 `self.rect`。
3. 在 `Enemy.update()` 中让敌机按照 `self.speed` 向下移动，并同步 `self.rect.y`。
4. 在 `Enemy.is_out_of_screen()` 中判断敌机是否已经完全飞出屏幕底部，并返回 `True` 或 `False`。

解释说明：
- 可以将初始纵坐标设为 `-self.rect.height`，使敌机从屏幕正上方开始出现。
- 敌机完全离开底部时，`rect.top` 会大于屏幕高度。
- 可以使用 `random.randint()` 生成随机横坐标；
  `random.randint(a, b)`: 返回一个位于 `a` 和 `b` 之间的随机整数（包含 `a` 和 `b`）。


#### 2.2 管理敌机的生成、更新与清理

涉及文件：`game.py`

需要完成：

1. 创建用于管理所有敌机的 `enemies` 列表。
2. 收到 `ADD_ENEMY_EVENT` 时创建一个 `Enemy` 并加入 `enemies` 列表。
3. 每一帧更新列表中的所有敌机。
4. 将已经飞出屏幕的敌机从列表中移除。
5. 每一帧绘制列表中的所有敌机。

提示：敌机生成定时事件已经按照配置的时间间隔注册。
不要一边遍历列表一边直接 `remove()`；可以用**列表推导式**保留仍在屏幕内的对象。

---

**列表推导/生成式（List Comprehension）**

列表推导式是一种**简洁创建列表的方法**，可以通过遍历一个序列，并对每个元素进行处理或筛选，快速生成新的列表。

核心语法：

```python
new_list = [expression for item in old_list if condition]
```

等价于：

```python
new_list = []
for item in old_list:
    if condition:
        new_list.append(expression)
```

示例：

```python
arr1 = [1, -3, 6, -2, 7, 5, -4]

arr2 = [n for n in arr1 if n > 0]
print(arr2)   # [1, 6, 7, 5]

arr3 = [x * x for x in arr2]
print(arr3)   # [1, 36, 49, 25]
```

列表推导式让代码更加简洁，常用于**快速创建、转换和筛选列表**。


---

实现效果
- 游戏窗口能正常打开和关闭。
- 敌机生成时不会超出屏幕左右边界，并从屏幕上方开始出现。
- 敌机会以随机横坐标从屏幕上方不断出现并向下移动。
- 敌机完全离开屏幕后会被清理，长时间运行不会无限积累离屏对象。
- 本版本暂时没有子弹、得分、碰撞和死亡逻辑，能稳定运行。


### 三、v1.3：自动射击与战斗循环
在上一层基础上加入自动射击、子弹与敌机碰撞、对象消失，形成完整的核心战斗循环。

本部分需要拓展学习**rect的细节知识**, 具体看
[https://pygame.readthedocs.io/en/latest/rect/rect.html#](https://pygame.readthedocs.io/en/latest/rect/rect.html#)
- Virtual attributes
- Points of interest
- Colliding points
- Colliding rectangles

补充图解如下

<div align="center">
    <img src="./images/301.png" width="80%">
</div>

#### 3.1 实现子弹创建与发射

涉及文件：`settings.py`、`bullet.py`、`player.py`

需要完成：

1. 在配置文件中设置子弹图片路径和合适的飞行速度。
2. 创建子弹时，将子弹图片的底部中点放在玩家飞机的顶部中点。
3. 保存子弹的位置和速度。
4. 在 `Bullet.update()` 中让子弹向屏幕上方移动并同步 `rect`。
5. 在 `Bullet.is_out_of_screen()` 中判断子弹是否已经完全离开屏幕上方。
6. 在 `Player.shoot()` 中创建并返回一个 `Bullet` 对象。

提示：可使用 `rect.midbottom`、玩家的 `rect.centerx` 和 `rect.top` 对齐发射位置。

#### 3.2 管理自动射击与子弹生命周期

涉及文件：`game.py`、`settings.py`

需要完成：

1. 注册射击定时事件。
2. 收到 `SHOOT_EVENT` 时调用 `player.shoot()`，把返回的子弹加入 `bullets` 列表。
3. 每一帧更新并绘制全部子弹。
4. 清理完全飞出屏幕上方的子弹。
5. 只有游戏处于 `PLAYING` 状态时，才允许生成敌机和子弹。

#### 3.3 实现子弹与敌机碰撞

涉及文件：`game.py`

需要完成：

1. 遍历子弹和敌机，使用 `rect.colliderect()` 判断矩形是否相交。
2. 一颗子弹击中一个敌机后，该子弹和该敌机都应消失。
3. 一颗子弹在同一帧最多击中一个敌机。
4. 不要在嵌套遍历过程中直接修改原列表；先记录命中的对象，再统一过滤。

---

完成后效果
- 玩家飞机能够按固定时间间隔自动发射子弹。
- 子弹从玩家飞机顶部中央出现并向上移动。
- 离开屏幕的子弹会被清理。
- 子弹与敌机相交时，两者都消失。

### 四、v1.4：计分、精确碰撞与完整游戏流程

在前三个版本的基础上加入计分、精确碰撞、游戏状态和中英文文本切换，
使项目从“功能演示”升级为流程完整、可重复游玩的游戏。

#### 4.1 建立游戏状态与统一重置逻辑
涉及文件：`settings.py`、`game.py`

先在 `settings.py` 中定义三种**游戏状态常量**，分别表示游戏的不同阶段。
| 状态名称               | 状态值 | 游戏行为                             |
| ------------------ | --- | -------------------------------- |
| `STATUS_START`     | `0` | 开始界面，等待玩家开始游戏；不更新或生成战斗对象         |
| `STATUS_PLAYING`   | `1` | 游戏进行中；更新玩家、敌机、子弹等对象，并执行碰撞检测等战斗逻辑 |
| `STATUS_GAME_OVER` | `2` | 游戏结束；停止战斗逻辑，显示结束界面，等待玩家重新开始      |

需要完成：
1. 实现 `reset_game()`，返回新玩家、空敌机列表、空子弹列表和 `0` 分。
2. 程序已在启动时调用 `reset_game()`，并将初始状态设为 `STATUS_START`。
3. 在 `STATUS_START` 或 `STATUS_GAME_OVER` 状态按下任意键时，重新调用 `reset_game()` 并进入 `STATUS_PLAYING`。
4. 仅在 `STATUS_PLAYING` 状态生成战斗对象；通过不同状态的流程分支，避免其他状态继续执行战斗逻辑。

#### 4.2 完成精确碰撞、游戏结束与计分
涉及文件：`player.py`、`enemy.py`、`game.py`

**玩家与敌机碰撞**
需要完成：
1. 根据玩家和敌机的透明图片分别创建碰撞遮罩 `mask`。
2. 使用已经提供的 `is_mask_collision(sprite1, sprite2)` 检查玩家与敌机的非透明部分是否重叠。
3. 在游戏进行中遍历敌机；玩家与任意敌机发生像素级碰撞后，立即进入 `STATUS_GAME_OVER`。

> 拓展解释（不用理解）：
> - 遮罩碰撞的偏移量应为第二个对象的 `rect` 坐标减去第一个对象的 `rect` 坐标。
> - 子弹与敌机仍然使用 v1.3 中的矩形碰撞，不需要改成像素级碰撞。

**计分**
1. 分数由 `reset_game()` 初始化为 `0`，每消灭一架敌机增加 `10` 分。
2. 命中后记录子弹和敌机、增加分数并结束该子弹的检测，确保一次命中只计分一次。
3. 重新开始游戏时，通过 `reset_game()` 将分数归零。

#### 4.3 完善状态界面、文本切换与退出流程

涉及文件：`game.py`、`settings.py`

**文本配置**

`settings.py` 已集中提供标题、操作提示、结束提示和分数等中英文文本。绘制界面时应统一通过 `get_text()` 获取文字；修改当前语言配置后，全部界面文字应一起切换。

**状态界面**

| 状态 | 界面内容 |
| --- | --- |
| `STATUS_START` | 绘制背景、玩家、游戏标题和开始提示 |
| `STATUS_PLAYING` | 绘制全部战斗对象，并在右上角显示当前分数 |
| `STATUS_GAME_OVER` | 保留本局结束时的画面，显示游戏结束、最终得分和重新开始提示 |

绘制函数只负责显示，不修改对象位置、分数或游戏状态。开始和结束界面绘制完成后，使用 `continue` 跳过战斗逻辑。

**刷新与退出**

1. 每种状态都要刷新画面并限制帧率。
2. 退出事件处理和 `pygame.quit()` 已经提供，应确保新增的状态分支不影响关闭窗口。

---

最终效果：

- 启动时显示开始界面，按任意键后才开始战斗。
- 玩家图片的透明区域不会造成明显的“隔空死亡”。
- 玩家与敌机实际碰撞后进入游戏结束界面。
- 结束界面显示本局最终分数，战斗对象停止更新和生成。
- 按任意键可以开始全新一局，玩家位置、敌机、子弹和分数全部重置。
- 切换语言配置后，标题、提示和分数等文字一致切换。
- 关闭窗口后程序正常退出，无报错。

### 五、v1.5：游戏背景与背景音乐

在 v1.4 已经具备完整游戏流程的基础上，进一步优化游戏氛围。
本阶段不改变玩家移动、敌机生成、自动射击、碰撞检测和计分规则，
主要通过游戏背景与背景音乐增强战斗氛围。

#### 5.1 添加游戏背景

涉及文件：`settings.py`、`game.py`、背景图片素材

本阶段使用 `assets/bg1.png` 作为固定背景，使游戏画面更符合空战主题。

**背景素材要求**

1. 背景图片使用 `assets/bg1.png`。
2. 图片尺寸为 `500 × 1024`，能够覆盖 `500 × 800` 的游戏窗口；超出窗口的部分由 Pygame 自动裁剪，无需额外缩放。
3. 背景应放在所有游戏对象和界面文字的下方。

**需要完成**

1. 在 `settings.py` 中配置背景图片路径。
2. 创建游戏窗口后加载一次背景图片。
3. 在绘制敌机、子弹、玩家和文字之前绘制背景图片，替换原来的纯色背景。
4. 开始、游戏中和游戏结束三种状态共用同一个绘制函数，因此都应显示该背景。

本阶段只实现固定背景，不需要缩放或滚动背景，也不改变原有游戏逻辑。

#### 5.2 实现连续滚动背景

涉及文件：`settings.py`、`game.py`、背景图片素材

本阶段在固定背景的基础上实现连续滚动效果，使游戏画面更有飞行感。

**滚动效果**
1. 使用同一张背景图片绘制两次，两张图片上下首尾相接。
2. 两张图片共用变量 `background_y` 记录纵向偏移量，并以 `BACKGROUND_SPEED` 的速度向下移动。
3. 当偏移量达到背景图片高度时，将其重置为 `0`，形成无缝循环。
4. 背景应放在所有游戏对象和界面文字的下方。
5. 开始界面和游戏过程中背景持续滚动；玩家阵亡后背景停止滚动。

**需要完成**

1. 在 `settings.py` 中配置背景图片路径和滚动速度。
2. 创建游戏窗口后加载一次背景图片，并记录背景的纵向偏移量。
3. 在主循环中更新背景偏移量，游戏结束后不再更新。
4. 分别在 `background_y` 和 `background_y - height` 位置绘制同一张背景图片。
5. 重新开始游戏后，背景应恢复滚动。

本阶段只改变背景的显示效果，不改变玩家、敌机、子弹、碰撞和计分等原有游戏逻辑。

#### 5.3 添加背景音乐与游戏音效

为 `game5` 添加背景音乐和战斗音效。  
涉及文件：`game5/settings.py`、`game5/game.py`。

本任务只添加声音功能，不修改游戏规则。

**音频素材**

| 用途 | 文件 |
| --- | --- |
| 开始界面音乐 | `assets/audio/space_adventure_clip.ogg` |
| 游戏中音乐 | `assets/audio/through_space_clip.ogg` |
| 敌机爆炸 | `assets/audio/boom-and-rattle.wav` |
| 玩家坠毁 | `assets/audio/explosion.wav` |

在 `settings.py` 中统一配置音频路径和音量：开始音乐 `0.15`、游戏音乐 `0.25`、敌机爆炸 `0.25`、玩家坠毁 `0.20`。

**播放规则**

- **开始界面**：循环播放开始界面音乐。
- **游戏开始或重新开始**：切换并循环播放游戏中音乐。
- **敌机被击毁**：子弹命中并消灭敌机时，播放一次敌机爆炸音效。
- **玩家坠毁**：播放一次坠毁音效，停止背景音乐并进入游戏结束界面。

背景音乐使用 `pygame.mixer.music`，音效使用预先加载的 `pygame.mixer.Sound`。音乐切换和坠毁音效只能在游戏状态改变时触发一次。

**实现效果**

- 开始界面和游戏中的音乐能够正确播放和切换；
- 敌机爆炸和玩家坠毁音效触发准确且不重复；
- 游戏结束后背景音乐停止；
- 多次开始、死亡和重新开始后，声音状态仍然正常；

#### 5.4 动态效果优化：爆炸特效

为敌机被击毁的过程添加爆炸动画，使碰撞反馈更加直观，提升战斗画面的表现力。

本任务只优化敌机被击毁时的视觉效果，不改变子弹伤害、碰撞判定、得分、敌机生成和玩家生命等原有游戏逻辑。

**爆炸动画素材**

爆炸动画由 5 张尺寸相同的 PNG 图片组成，按编号从小到大依次播放：

| 播放顺序 | 素材文件 |
| --- | --- |
| 第 1 帧 | `assets/explosion1.png` |
| 第 2 帧 | `assets/explosion2.png` |
| 第 3 帧 | `assets/explosion3.png` |
| 第 4 帧 | `assets/explosion4.png` |
| 第 5 帧 | `assets/explosion5.png` |

每张图片的尺寸均为 `86 × 84` 像素。所有素材应在游戏初始化阶段统一加载，避免在主循环中重复读取文件。

在 `settings.py` 中使用 `EXPLOSION_IMAGES` 按顺序保存图片路径，并使用 `EXPLOSION_FRAME_INTERVAL` 控制切换图片的间隔。建议每张图片显示 `4` 个游戏帧；间隔越大，动画播放越慢。

**核心数据与实现思路**

游戏使用 `explosions` 列表保存当前所有正在播放的爆炸。每个爆炸需要独立记录以下三项数据：

- `center`：敌机被击毁时的中心位置。创建动画后保持不变，保证爆炸不会跟随其他对象移动；
- `frame`：当前显示的图片编号，从 `0` 开始，对应爆炸图片列表中的第一张图片；
- `timer`：当前图片已经持续显示的游戏帧数，用于判断何时切换到下一张图片。

主循环每执行一次，就将每个爆炸的 `timer` 增加 `1`。当 `timer` 达到 `EXPLOSION_FRAME_INTERVAL` 时，将 `timer` 重置为 `0`，同时把 `frame` 增加 `1`。当 `frame` 达到爆炸图片总数时，表示 5 张图片已经全部播放完成，应从 `explosions` 列表中移除该动画。

绘制时先根据 `frame` 从 `explosion_images` 中取得当前图片，再使用 `get_rect(center=...)` 让图片中心与保存的 `center` 重合。这样即使爆炸图片和敌机图片尺寸不同，特效也能稳定显示在敌机被击毁的位置。

每次击毁敌机都向 `explosions` 中添加一份新的动画数据，因此多个爆炸可以在同一帧中分别更新和绘制，互不影响。

**播放规则**

1. 当玩家子弹命中并击毁敌机时，在敌机消失的位置创建一次爆炸动画。
2. 爆炸动画的中心应与敌机被击毁时的中心位置保持一致，播放期间不跟随其他对象移动。
3. 5 张图片按照 `explosion1.png` 到 `explosion5.png` 的顺序播放，每张图片显示若干游戏帧，避免动画播放过快。
4. 最后一帧播放完成后，自动移除该爆炸动画，不在屏幕中残留，也不重复播放。
5. 同一架敌机只能触发一次爆炸；多个敌机同时被击毁时，各自的爆炸动画应能够独立播放。
6. 爆炸动画只负责画面展示，不参与碰撞检测，也不会再次造成伤害或增加得分。
7. 爆炸音效仍按照 5.3 节的规则播放，并与爆炸动画同时触发。

**需要完成**

1. 在配置文件中设置爆炸图片路径、动画切换间隔等参数。
2. 加载并保存全部爆炸序列帧。
3. 为每个爆炸动画记录 `center`、`frame` 和 `timer`，分别表示中心位置、当前图片和切帧进度。
4. 在子弹与敌机的碰撞处理完成后，根据被击毁敌机的位置创建爆炸动画。
5. 在游戏主循环中持续更新并绘制当前仍在播放的爆炸动画。
6. 动画播放结束后及时清理对应对象；重新开始游戏时清空尚未结束的爆炸动画。

**绘制层级**

爆炸动画应绘制在背景之上，并显示在敌机、子弹等战斗对象附近。界面文字、得分和游戏结束提示仍应位于最上层，避免被特效遮挡。

**实现效果与验收标准**

- 子弹击毁敌机后，敌机立即消失，并在原位置完整播放一次爆炸动画；
- 动画帧顺序正确、播放速度自然，没有明显闪烁或跳帧；
- 爆炸图片位置稳定，不会因图片尺寸产生明显偏移；
- 同时击毁多个敌机时，可以同时显示多个互不影响的爆炸动画；
- 每架被击毁的敌机只计分一次，并只触发一次爆炸动画和爆炸音效；
- 动画结束后能够自动清理，长时间运行不会持续积累无效对象；
- 游戏结束或重新开始后，不会残留上一局尚未播放完的爆炸画面。


### 总结、核心流程回顾与验收

每一帧应遵循下面的基本顺序：

```text
读取并处理事件
       ↓
根据当前游戏状态决定是否开始/重置/生成对象
       ↓
根据游戏状态更新滚动背景
       ↓
PLAYING：更新玩家、敌机、子弹和爆炸动画
       ↓
清理已经离开屏幕的对象
       ↓
检查子弹—敌机碰撞，计分并触发爆炸效果
       ↓
检查玩家—敌机碰撞并决定是否结束
       ↓
依次绘制背景、游戏对象、爆炸动画和界面文字
       ↓
刷新窗口并限制帧率
```

注意：绘制只负责“显示”，不应偷偷改变分数、位置或游戏状态；对象更新和规则判断应在绘制之前完成。

---

**实现功能清单**

提交前逐项测试，并在确认后打勾：

- [ ] 能从项目根目录正常启动游戏。
- [ ] 关闭窗口能够退出，没有异常信息。
- [ ] 方向键和 `W/A/S/D` 都能移动玩家。
- [ ] 玩家在四条边界和四个角都不会越界。
- [ ] 敌机不会生成到屏幕左右边界之外。
- [ ] 敌机和子弹离屏后会被清理。
- [ ] 子弹始终从玩家顶部中央发射。
- [ ] 同一颗子弹不会一次消灭多架敌机。
- [ ] 击中一架敌机只增加一次分数。
- [ ] 玩家碰撞后立即进入结束状态。
- [ ] 开始和结束界面不会继续产生敌机或子弹。
- [ ] 连续重新开始多次，数据都能正确归零。
- [ ] 中文、英文两种配置均可正常显示。
- [ ] 开始、游戏中和游戏结束界面都能正确显示背景。
- [ ] 背景能够连续滚动，游戏结束后停止，重新开始后恢复。
- [ ] 开始界面和游戏中的背景音乐能够正确播放和切换。
- [ ] 敌机被击毁和玩家坠毁时，音效只播放一次。
- [ ] 敌机被击毁后，爆炸动画在原位置按顺序完整播放一次。
- [ ] 多个爆炸动画可以同时播放，并在结束后自动清理。
- [ ] 游戏结束或重新开始后，不会残留上一局的爆炸动画。